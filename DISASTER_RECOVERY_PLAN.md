# خطة استعادة البيانات بعد الكوارث واستمرارية الأعمال (Disaster Recovery & Backup Plan)

تضع هذه الخطة الإجراءات والخطوات التقنية المعتمدة لضمان أمان البيانات واستمرارية عمل نظام إدارة معرض السيارات تحت أي ظروف طارئة (مثل تلف قاعدة البيانات، تعطل الخادم، أو الأخطاء البشرية)، مع ضمان حماية السجلات المحاسبية والمالية بشكل مطلق ومنع حذفها.

---

## 1. استراتيجية النسخ الاحتياطي (Backup Strategy)

لضمان تغطية كافة البيانات الحيوية، ينقسم النسخ الاحتياطي إلى محورين:

### أ. النسخ الاحتياطي لقاعدة البيانات (PostgreSQL Dump)
- **الأداة المستعملة**: أداة `pg_dump` الرسمية لتوليد ملف SQL متكامل يحتوي على بنية الجداول والبيانات.
- **التكرار**: يتم توليد النسخة الاحتياطية **كل 6 ساعات** بشكل آلي.
- **الصيغة**: ملف مضغوط بصيغة `.sql.gz` أو بتنسيق JSON الموحد للنظام لتسهيل النقل.

### ب. النسخ الاحتياطي للمستندات والملفات (Media & Documents Backup)
- **الأداة المستعملة**: أداة ضغط الملفات وتدويرها لتجميع مجلدات الـ uploads والمجلد الخاص `storage/private`.
- **التكرار**: يتم أخذ نسخة احتياطية **مرة واحدة يومياً** (عند الساعة 02:00 صباحاً).

---

## 2. سياسة الحفظ والتصفية (Retention Policy)

لتفادي استهلاك مساحة التخزين على الخادم بشكل كامل، يتم تطبيق سياسة التصفية التلقائية التالية للنسخ الاحتياطية المخزنة محلياً:

- **النسخ الاحتياطية لكل 6 ساعات**: يتم الاحتفاظ بها لمدة **3 أيام** (12 نسخة).
- **النسخ الاحتياطية اليومية**: يتم الاحتفاظ بها لمدة **30 يوماً** (30 نسخة).
- **النسخ الاحتياطية الأسبوعية**: يتم الاحتفاظ بها لمدة **12 أسبوعاً** (12 نسخة).
- **النسخ الاحتياطية الشهرية**: يتم الاحتفاظ بها لمدة **سنة كاملة** (12 نسخة).

---

## 3. سكربت النسخ الاحتياطي التلقائي لـ PostgreSQL

فيما يلي سكربت Bash جاهز ومعد للنشر على خادم الإنتاج لأتمتة عملية أخذ النسخ الاحتياطية وتصفية النسخ القديمة بناءً على سياسة الحفظ:

```bash
#!/bin/bash
# backup_postgres.sh
# ==========================================
# سكربت النسخ الاحتياطي التلقائي لقاعدة بيانات النظام وتصفية الملفات القديمة

BACKUP_DIR="/var/lib/docker/volumes/car_showroom_management_app_backups/_data"
DB_CONTAINER="car-showroom-db"
DB_USER="showroom_admin"
DB_NAME="showroom_production"
DATE=$(date +%Y-%m-%d_%H%M%S)
BACKUP_FILE="${BACKUP_DIR}/db_backup_${DATE}.sql.gz"

echo "=== بدء عملية النسخ الاحتياطي [$(date)] ==="

# التأكد من وجود مجلد النسخ الاحتياطي
mkdir -p ${BACKUP_DIR}

# تنفيذ النسخ الاحتياطي المضغوط من داخل الحاوية
docker exec ${DB_CONTAINER} pg_dump -U ${DB_USER} -d ${DB_NAME} | gzip > ${BACKUP_FILE}

if [ $? -eq 0 ]; then
    echo "نجح النسخ الاحتياطي: ${BACKUP_FILE}"
else
    echo "خطأ: فشلت عملية النسخ الاحتياطي لقاعدة البيانات!"
    exit 1
fi

# تطبيق سياسة الحفظ: حذف النسخ القديمة التي مضى عليها أكثر من 30 يوماً
echo "تصفية النسخ الاحتياطية القديمة..."
find ${BACKUP_DIR} -name "db_backup_*.sql.gz" -mtime +30 -exec rm {} \;

echo "=== انتهت العملية بنجاح [$(date)] ==="
```

### جدولة السكربت عبر Cron Job:
لجدولة السكربت ليعمل كل 6 ساعات تلقائياً، أضف السطر التالي إلى ملف جدولة المهام الخاص بالنظام (`crontab -e`):
```cron
0 */6 * * * /bin/bash /opt/showroom/scripts/backup_postgres.sh >> /var/log/showroom_backup.log 2>&1
```

---

## 4. خطوات الاستعادة السريعة (Recovery Steps)

عند حدوث أي طارئ يستلزم استعادة قاعدة البيانات إلى نقطة زمنية سابقة، اتبع الخطوات التالية بدقة:

### أ. إيقاف خادمي Frontend و Backend مؤقتاً لتجنب تداخل البيانات:
```bash
docker compose stop frontend backend
```

### ب. فك الضغط عن نسخة قاعدة البيانات المستهدفة:
قم باختيار النسخة الاحتياطية المطلوبة من مجلد backups (مثال: `db_backup_2026-06-10_120000.sql.gz`) وفك ضغطها:
```bash
gunzip -c /var/lib/docker/volumes/car_showroom_management_app_backups/_data/db_backup_2026-06-10_120000.sql.gz > /tmp/restore_db.sql
```

### ج. تنظيف وتفريغ قاعدة البيانات الحالية:
```bash
docker compose exec db psql -U showroom_admin -d showroom_production -c "DROP SCHEMA public CASCADE; CREATE SCHEMA public;"
```

### د. استيراد النسخة الاحتياطية وتطبيقها:
```bash
docker compose exec -T db psql -U showroom_admin -d showroom_production < /tmp/restore_db.sql
```

### هـ. إعادة تشغيل الخدمات:
```bash
docker compose start backend frontend
```

### و. التحقق من سلامة البناء وعمل السجلات:
قم بفحص السجلات للتأكد من خلوها من الأخطاء:
```bash
docker compose logs -f backend
```

---

## 5. سيناريوهات الكوارث الشائعة وكيفية إدارتها (Risk Scenarios)

| السيناريو | الأثر التقني | الإجراء العاجل المتخذ |
| :--- | :--- | :--- |
| **تلف كامل للخادم أو نظام التشغيل** | توقف النظام بالكامل وفقدان إمكانية الوصول للآلة المستضيفة. | 1. تهيئة خادم جديد فورا بنفس المواصفات.<br>2. تنزيل الكود البرمجي وتطبيق إعدادات Docker.<br>3. جلب آخر نسخة احتياطية مشفرة مخزنة سحابياً واستعادتها كما هو موضح بالبند 4. |
| **هجوم فدية أو اختراق أمني** | تشفير قاعدة البيانات أو تعطل ملفاتها الحيوية. | 1. عزل الخادم فوراً عن الشبكة الخارجية لمنع انتشار الهجوم.<br>2. إيقاف كافة الحاويات ومراجعة سجلات الشبكة للتعرف على طريقة الاختراق.<br>3. سد الثغرة وإعادة بناء الحاويات بالكامل من مستودع الكود الأصلي وتنزيل قاعدة البيانات النظيفة. |
| **حذف ملفات العملاء أو الصور بالخطأ** | عدم قدرة النظام على عرض صور السيارات أو وثائق الهوية المرفوعة. | 1. استرجاع مجلد `uploads` و `private` من آخر نسخة احتياطية يومية وتطبيقها في الـ Volumes المخصصة لها. |
| **تعارض في ترقيات قاعدة البيانات** | فشل الخادم الخلفي في الاستعلام وتوقف الجداول بسبب تعديلات فاشلة. | 1. استعادة الجداول باستخدام سكربتات الترحيل والتراجع (Alembic/Flask-Migrate) عن طريق الأمر `flask db downgrade`. |

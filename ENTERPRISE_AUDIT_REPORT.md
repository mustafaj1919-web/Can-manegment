# تقرير تدقيق جاهزية المؤسسات الشامل (Enterprise Readiness Audit Report)

**النظام:** نظام إدارة معرض السيارات (Car Showroom Management)  
**تاريخ التقرير:** 10 يونيو 2026  
**الجهة المنفذة:** Antigravity (فريق التطوير والجاهزية)  
**حالة التدقيق:** مكتمل  

---

## 1. التقييم الإجمالي لإنتاجية النظام (Final Production Score)

### **71 / 100**

> [!WARNING]
> على الرغم من نجاح عملية بناء نسخة الإنتاج (`npm run build`) واجتياز جميع اختبارات الوحدة الحالية بنسبة 100%، إلا أن النظام يحتوي على ثغرات أمنية ذات خطورة عالية وتصميم غير مكتمل لفصل بيانات الفروع، بالإضافة إلى ضعف في أداء تصيير الواجهات الأمامية والتحميل الكلي. لا يُنصح بإطلاق النظام كجاهز للإنتاج (Production Ready) قبل معالجة المسائل ذات الخطورة الحرجة والعالية.

---

## 2. مصفوفة المشاكل بحسب خطورتها (Audit Findings Matrix)

### المشاكل الحرجة (Critical Issues) — العدد: 1
1. **تسريب هويات ومستندات العملاء الحساسة دون مصادقة (Sensitive Data Exposure):**
   * **الوصف:** يتم حفظ مستندات العملاء (الهويات، رخص القيادة، العقود) في المجلد العام `static/uploads/customers` المخدم مباشرة من Flask. أي شخص يمتلك أو يخمن رابط الملف (الذي يتبع نمط UUID) يمكنه تنزيله مباشرة من المتصفح دون الحاجة لجلسة نشطة أو تسجيل دخول.
   * **الأثر:** تسريب بيانات شخصية حساسة (PII) وخرق قوانين حماية البيانات.

---

### المشاكل العالية (High Issues) — العدد: 5
1. **ثغرات التفويض المباشر وتعديل بيانات CRM للفروع الأخرى (Cross-Branch IDOR in CRM):**
   * **الملفات المتأثرة:** [app.py](file:///d:/System/car_showroom_management/backend/app.py#L4053-L4077)
   * **الوصف:** مسارات تعديل وحذف تفاعلات العملاء (`PUT` / `DELETE` `/api/crm/interactions/<id>`) لا تتحقق من أن السجل ينتمي للفرع المتاح للمستخدم الحالي عبر التابع `branch_allowed()`.
   * **الأثر:** يمكن لموظف مبيعات في فرع (أ) تعديل أو حذف تفاعلات وسجلات موظفي فرع (ب).

2. **العبث بصفقات Pipeline وتغيير مراحلها بين الفروع (Cross-Branch IDOR in Pipeline):**
   * **الملفات المتأثرة:** [app.py](file:///d:/System/car_showroom_management/backend/app.py#L4238-L4287)
   * **الوصف:** مسارات تعديل، حذف، وتغيير مرحلة الصفقات في الـ Pipeline لا تقوم بفحص الفرع المسموح به للمستخدم برمجياً.
   * **الأثر:** نقل صفقات معرض آخر أو تغيير أسعارها المتوقعة أو حذفها بالكامل من قبل موظف غير مخول.

3. **كشف وتعديل بيانات موظفي الفروع الأخرى (Cross-Branch IDOR in Employees):**
   * **الملفات المتأثرة:** [app.py](file:///d:/System/car_showroom_management/backend/app.py#L8394-L8431)
   * **الوصف:** مسارات عرض تفاصيل الموظف وتحديث بياناته وحذفه (`GET` / `PUT` / `DELETE` `/api/employees/<id>`) لا تحتوي على فحص `branch_allowed()`.
   * **الأثر:** كشف معلومات شخصية حساسة لموظفي الفروع المنافسة أو تعديل حالتهم التشغيلية.

4. **سحب إيصالات الدفع والقبض من فروع أخرى (Payment Receipts Access Leak):**
   * **الملفات المتأثرة:** [app.py](file:///d:/System/car_showroom_management/backend/app.py#L7655-L7680)
   * **الوصف:** مسار استرجاع بيانات سند القبض المالي (`GET` `/api/payments/<id>/receipt`) يعتمد فقط على معرف الدفعة ولا يقوم بالتحقق من فرع المبيعات المرتبط بالدفعة.
   * **الأثر:** تسريب تفاصيل المبالغ والعملاء لعمليات مالية تخص فروعاً أخرى.

5. **عرض معلومات البنية التحتية للمخدم للمستخدمين العاديين (Infrastructure Info Exposure):**
   * **الملفات المتأثرة:** [app.py](file:///d:/System/car_showroom_management/backend/app.py#L8149-L8157)
   * **الوصف:** مسار تشخيص حالة السكنر المحلي `/api/scanner/status` متاح لأي مستخدم مسجل (`@api_login_required` فقط) دون اشتراط صلاحيات إدارية، ويعرض معلومات فنية حول بيئة تشغيل الخادم.

---

### المشاكل المتوسطة (Medium Issues) — العدد: 4
1. **خطر انهيار المخدم بسبب قنابل الملفات المضغوطة (Zip Bomb Vulnerability):**
   * **الملفات المتأثرة:** [backup_utils.py](file:///d:/System/car_showroom_management/backend/backup_utils.py#L301-L306)
   * **الوصف:** آلية استعادة النسخ الاحتياطية تقوم باستخراج الملفات مباشرة عبر `zip_file.extractall()` دون التحقق المسبق من عدد الملفات أو حجمها الأقصى غير المضغوط.
   * **الأثر:** استهلاك كامل مساحة القرص أو الذاكرة وانهيار النظام عند رفع أرشيف مفخخ (Denial of Service).

2. **خطر فقدان البيانات بالكامل عند فشل عملية الاسترجاع (Data Loss Risk on Restore Failure):**
   * **الملفات المتأثرة:** [backup_utils.py](file:///d:/System/car_showroom_management/backend/backup_utils.py#L141-L151)
   * **الوصف:** تبدأ عملية الاستعادة بمسح وتصفير الجداول بالكامل (`TRUNCATE`) قبل بدء إدخال البيانات المستعادة من ملف الـ JSON.
   * **الأثر:** إذا حدث خطأ برمجي أو انقطاع في الاتصال بقاعدة البيانات أثناء إدخال السجلات، تظل قاعدة البيانات فارغة تماماً وتفقد البيانات السابقة دون وجود آلية تراجع تلقائية.

3. **غياب الترويسات الأمنية الأساسية عن واجهة Next.js (Missing Security Headers):**
   * **الملفات المتأثرة:** [next.config.js](file:///d:/System/car_showroom_management/frontend/next.config.js)
   * **الوصف:** لا يرسل خادم Next.js ترويسات الحماية الهامة مثل CSP (Content Security Policy) أو X-Frame-Options أو X-Content-Type-Options لحماية المتصفح.

4. **تكرار طلب العلاقات في جداول البيانات (N+1 Query Problem):**
   * **الوصف:** استعلامات جلب السيارات والمبيعات والمشتريات في `app.py` لا تستخدم التحميل المسبق للعلاقات (Eager Loading) عبر `joinedload` أو `subqueryload`؛ مما يؤدي إلى إرسال عشرات الاستعلامات الفرعية لقاعدة البيانات عند عرض الجداول.

---

### المشاكل المنخفضة (Low Issues) — العدد: 2
1. **كشف تفاصيل الإصدارات التشغيلية (Version Disclosure):**
   * مسار `/api/version` متاح للمستخدمين المسجلين ويكشف رقم النسخة الحالي للنظام.
2. **أعمدة الفروع قابلة للقيمة الفارغة (Nullable Branch IDs):**
   * يسمح تصميم قاعدة البيانات الحالية لـ 19 جدولاً بتسجيل بيانات بدون فرع (`branch_id` يقبل NULL)، مما يعرض سلامة التقارير للخطر في حال حدوث خطأ برمجي.

---

## 3. تفاصيل نتائج التدقيق التفصيلية (Detailed Findings)

### أولاً: التدقيق الأمني (Security Audit)
* **المصادقة والتفويض:** جميع المسارات محمية بالتحقق من الجلسة، ولكن هناك ضعف شديد في التفويض المعتمد على الفرع (Branch Authorization) مما يسهل هجمات IDOR.
* **XSS / CSRF / SQLi:** الحماية ممتازة؛ CSRF مفعل بشكل متين على جميع الطلبات المعدلة، واستخدام SQLAlchemy ORM يمنع حقن SQL بالكامل.
* **رفع الملفات:** آلية التحقق من نوع الملف حقيقية وتستخدم Magic Bytes لمنع رفع الملفات التنفيذية، ولكن المجلد المستهدف عام وثغرة كشف مستندات الهوية تمثل خطراً قانونياً داهماً.
* **المتغيرات البيئية:** آمنة ويتم قراءتها من `.env` المستثنى من Git. مفتاح الجلسة `SECRET_KEY` يتم توليده وتدويره بشكل دوري.

### ثانياً: تدقيق الأداء والواجهة (Performance Audit)
* ** Largest Contentful Paint (LCP):** متأثر سلباً بسبب التحميل المتزامن والثابت لمكتبة الرسوم البيانية الثقيلة `recharts` ومكتبة معالجة الإكسل `write-excel-file` ضمن الحزمة الأولية للواجهة.
* **Cumulative Layout Shift (CLS):** يلاحظ حدوث قفزات بصرية (Layout Shifts) في صفحات الـ Dashboard والتقارير عند جلب البيانات الرسومية، لعدم حجز أبعاد ثابتة لعناصر الرسوم البيانية قبل تحميلها.
* **الحل المقترح:** تطبيق Lazy Loading و dynamic imports مع خيار `ssr: false` للمكونات المحتوية على رسوم بيانية ثقيلة لتقليل حجم الـ Bundle.

### ثالثاً: تدقيق قاعدة البيانات (Database Audit)
* **Indexes:** ممتازة وموزعة بشكل ذكي على الفروع وحالة المبيعات والسيارات والتواريخ، مما يسرع عمليات البحث والتصفية.
* **Foreign Keys:** المفاتيح الأجنبية معرفة بشكل سليم ولكن تفتقد إلى قيود الحذف المتتالي (Database-level Cascades) حيث يعتمد النظام بدلاً من ذلك على معالجة التطبيق البرمجي للـ relationships.
* **Transaction Safety:** العمليات المالية الحساسة محاطة بـ `try...except` وتقوم بعمل `db.session.rollback()` بشكل متسق يضمن عدم حفظ قيود مالية مشوهة.

### رابعاً: التدقيق المحاسبي (Accounting Audit)
* **سلامة القيود:** متوافقة 100% مع المعايير المحاسبية. لا يوجد أي مسار برمجي يسمح بحذف القيود اليومية أو السندات المالية (التزاماً تاماً بالقاعدة الصارمة لمنع الحذف).
* **عكس القيود (Reversal):** يتم بطريقة سليمة جداً عبر إنشاء قيد يومية عكسي بالكامل مقلوب الأطراف مع الإشارة للمرجع الأصلي وتحديث الحالة لـ `reversed`.
* **أرصدة الصندوق والبنوك والعملاء:** تتم مطابقتها دفترياً بشكل دقيق بناءً على ترحيل القيود اليومية المتوازنة.
* **الفترات المالية:** محمي بنظام إغلاق الفترات `check_period_lock` الذي يمنع التعديل والترحيل للتواريخ المغلقة.

### خامساً: تدقيق الفروع المتعددة (Multi-Branch Audit)
* **عزل البيانات:** غير كافٍ. بالرغم من وجود `scoped_query` إلا أن هناك ثغرات اختراق فروع مباشرة في تفاعلات العملاء، صفقات البيع، الموظفين، البحث العام، وبعض التقارير المالية المشتركة.

### سادساً: النسخ الاحتياطي والاستعادة (Backup & Recovery)
* **النسخ:** ممتاز، يعتمد على تصدير الجداول إلى ملف JSON مستقل عن محرك قاعدة البيانات، مما يسهل عمليات النقل والترحيل.
* **الاستعادة:** يحتوي على نظام Topological Sort ممتاز لمنع تعارض المفاتيح الأجنبية أثناء الاستيراد، ولكن يعيبه تفريغ قاعدة البيانات مسبقاً قبل التأكد من اكتمال القراءة.

---

## 4. تدقيق التغطية البرمجية للاختبارات (Test Coverage Audit)

* **نسبة التغطية الكلية الحالية للمشروع (Total Coverage):** **51%**
* **نسبة التغطية التفصيلية حسب الملفات الفعالة:**

| اسم الملف | عدد السطور البرمجية | السطور غير المغطاة | نسبة التغطية الحالية | الحالة والتقييم |
|:---|:---:|:---:|:---:|:---|
| [models.py](file:///d:/System/car_showroom_management/backend/models.py) | 432 | 1 | **99%** | ممتاز جداً |
| [accounting.py](file:///d:/System/car_showroom_management/backend/accounting.py) | 145 | 16 | **89%** | ممتاز جداً |
| [config.py](file:///d:/System/car_showroom_management/backend/config.py) | 65 | 18 | **72%** | مقبول |
| [backup_utils.py](file:///d:/System/car_showroom_management/backend/backup_utils.py) | 211 | 113 | **46%** | ضعيف |
| [app.py](file:///d:/System/car_showroom_management/backend/app.py) | 4688 | 3043 | **35%** | ضعيف جداً |

### **خطة الوصول لنسبة تغطية تفوق 80%+:**

1. **كتابة اختبارات تكاملية شاملة لـ API المسارات (Integration API Testing):**
   * يجب كتابة اختبارات آلية تستدعي المسارات الموجودة في `app.py` وتغطي حالات النجاح والفشل، والتحقق من الصلاحيات لكل دور (Owner, Admin, Viewer, Sales). هذا سيرفع تغطية `app.py` من 35% إلى 80% مباشرة.
2. **اختبارات آلية للنسخ الاحتياطي والاستعادة (Automated Backup Tests):**
   * كتابة اختبارات محاكاة لعمليات إنشاء النسخ الاحتياطية وفك الضغط واستعادة الجداول والتحقق من سلامة البيانات بعدها لتغطية سطور `backup_utils.py` بشكل كامل.
3. **تغطية سيناريوهات الأخطاء (Edge Case Coverage):**
   * اختبار محاولات تسجيل الدخول الخاطئة وقفل الحسابات التدريجي، ومحاولات الوصول للتواريخ المغلقة محاسبياً.

---

## 5. الدين التقني المقترح معالجته (Technical Debt Roadmap)

### **المرحلة الأولى (فورية - خلال 48 ساعة):**
* نقل مجلد مستندات العملاء `customers/` خارج مجلد الـ `static` العام، وإعداد مسار خاص تنزيل محمي برمجياً وصلاحياً وجلسة.
* تطبيق فحص `branch_allowed()` على مسارات CRM والـ Pipeline والموظفين وإيصالات الدفع.
* تقييد مسار `/api/scanner/status` بصلاحية `manage_customers` أو `manage_database`.

### **المرحلة الثانية (خلال أسبوع):**
* إضافة ترويسات الحماية الأمنية (Security Headers) لـ Next.js.
* معالجة مشكلة N+1 Queries في المبيعات والمشتريات باستخدام `joinedload`.
* إعادة تصميم استعادة النسخ الاحتياطي بحيث تأخذ نسخة احتياطية سريعة تلقائية من قاعدة البيانات الحالية قبل مسح الجداول وتفريغها، للتراجع إليها في حال الفشل.

### **المرحلة الثالثة (خلال 3 أسابيع):**
* تطبيق الـ Lazy Loading للرسوم البيانية في الواجهة لتسريع وقت الـ LCP.
* كتابة اختبارات تكاملية إضافية لرفع التغطية لـ 80%+.

---

**معد التقرير:** Al.Dulimi99 (منشئ المشروع والمطور الأساسي)  
**الحالة الفنية:** جاهز للمراجعة واتخاذ القرارات  

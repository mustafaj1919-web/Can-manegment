# دليل إجراءات تشغيل وإدارة النظام (Operations Runbook)

يوضح هذا الدليل الإجراءات التشغيلية واليومية المعتمدة لإدارة وتشغيل وحماية نظام إدارة معرض السيارات في بيئة الإنتاج الفعّالة.

---

## 1. إجراءات بدء التشغيل (Startup Procedure)

لتشغيل كافة خدمات النظام بالترتيب الصحيح وضمان استقرار الاتصالات:

1. تأكد من وجود ملف المتغيرات البيئية المؤمن `.env` في المجلد الرئيسي.
2. قم بتشغيل الحاويات في الخلفية باستخدام Docker Compose:
```bash
docker compose up -d
```
3. انتظر لمدة 10 ثوانٍ لتهيئة قاعدة البيانات، ثم تحقق من أن جميع الحاويات تعمل بنجاح:
```bash
docker compose ps
```
4. تأكد من استجابة مؤشر فحص السلامة (Health Check API) للـ Backend:
```bash
curl -f http://localhost:5000/api/health
```
يجب أن يرجع الرد حالة الموثوقية وقاعدة البيانات: `{"status": "ok", "database": "ok", ...}` بنتيجة HTTP 200.

---

## 2. إجراءات إيقاف التشغيل بأمان (Shutdown Procedure)

عند الحاجة لإجراء صيانة دورية للخادم أو تعديل الإعدادات، يجب إيقاف الخدمات بانتظام لمنع تلف البيانات أو انقطاع الجلسات الحالية:

### أ. إيقاف الخدمات مؤقتاً (مع الحفاظ على تشغيل الحاويات):
```bash
docker compose stop
```

### ب. إيقاف وإزالة الحاويات بانتظام (دون حذف البيانات والـ Volumes):
```bash
docker compose down
```

### ج. إيقاف وتصفير وتفريغ الحاويات والـ Volumes تماماً (يستخدم فقط للتثبيت النظيف - يحذف قاعدة البيانات والملفات!):
```bash
docker compose down -v
```
> [!CAUTION]
> لا تستخدم الخيار `-v` في بيئة الإنتاج إلا إذا كنت متأكداً بنسبة 100% أنك تمتلك نسخة احتياطية خارجية وترغب في تصفير النظام بالكامل.

---

## 3. إجراءات النسخ الاحتياطي اليدوي (Backup Procedure)

على الرغم من وجود جدولة تلقائية للنسخ الاحتياطي كل 6 ساعات، يوصى بشدة بتشغيل نسخة احتياطية يدوية فورية قبل إجراء أي تحديثات برمجية أو ترقية في النظام:

1. تأكد من عمل حاوية قاعدة البيانات `car-showroom-db`.
2. قم بتشغيل الأمر التالي من خادم الإنتاج لتوليد ملف SQL مضغوط:
```bash
docker exec car-showroom-db pg_dump -U showroom_admin -d showroom_production | gzip > /var/lib/docker/volumes/car_showroom_management_app_backups/_data/db_backup_manual_$(date +%Y%m%d_%H%M%S).sql.gz
```
3. لنسخ المرفقات والملفات الحساسة للعملاء يدوياً:
```bash
tar -czf /var/lib/docker/volumes/car_showroom_management_app_backups/_data/private_files_backup_$(date +%Y%m%d_%H%M%S).tar.gz -C /var/lib/docker/volumes/car_showroom_management_private_storage/_data .
```

---

## 4. إجراءات استعادة البيانات والتحقق (Restore Procedure)

في حال تلف قاعدة البيانات أو الرغبة في التراجع عن إدخالات محاسبية خاطئة إلى نقطة زمنية سابقة:

1. قم بإيقاف الحاويات المرتبطة لتجنب عمليات الكتابة أثناء الاستعادة:
```bash
docker compose stop frontend backend
```
2. فك الضغط عن ملف النسخة الاحتياطية المطلوبة:
```bash
gunzip -c /var/lib/docker/volumes/car_showroom_management_app_backups/_data/db_backup_XXXX.sql.gz > /tmp/restore.sql
```
3. تنظيف قاعدة البيانات وتفريغها لإعادة البناء:
```bash
docker compose exec db psql -U showroom_admin -d showroom_production -c "DROP SCHEMA public CASCADE; CREATE SCHEMA public;"
```
4. استيراد كتل البيانات والـ schema بالكامل:
```bash
docker compose exec -T db psql -U showroom_admin -d showroom_production < /tmp/restore.sql
```
5. تشغيل الخدمات مجدداً:
```bash
docker compose start backend frontend
```
6. قم بتسجيل الدخول للنظام والتحقق الفوري من سلامة توازن ميزان المراجعة (Trial Balance) والقيود المحاسبية.

---

## 5. إجراءات الإغلاق المالي والشهري (Monthly & Yearly Closing)

لحماية البيانات المالية ومنع التلاعب أو إدخال قيود بأثر رجعي بعد انتهاء الفترات المحاسبية ومراجعتها من قبل الإدارة:

1. يتم مراجعة كافة المعاملات المالية، وسندات القبض والصرف، وجداول الأقساط للشهر المنتهي والتأكد من مطابقة النقدية بالصناديق.
2. يدخل المحاسب المسؤول إلى صفحة "الفترات المالية" (Fiscal Periods) في لوحة التحكم.
3. يقوم بالضغط على زر "إغلاق الفترة المالية" (Close Period) للشهر الحالي.
4. **التأثير التقني المحاسبي**:
   - يقوم النظام بتعديل حالة الفترة المالية إلى "Closed".
   - يفرض النظام قيداً برمجياً صارماً على جميع مسارات الإدخال (البيع، الشراء، اليومية، السندات) يحظر ويمنع تماماً تسجيل أي معاملة يقع تاريخها ضمن الفترة المغلقة.
   - **القيود المحاسبية مغلقة بالكامل ولا يمكن التعديل عليها أو حذفها نهائياً لضمان سلامة التدقيق المالي.**

---

## 6. إجراءات الاستجابة للطوارئ والأزمات (Emergency Procedures)

### أ. تعطل الخادم بالكامل وفقدان الاتصال:
1. قم بالدخول إلى لوحة تحكم الاستضافة السحابية (مثلاً AWS EC2 Console أو DigitalOcean).
2. قم بعمل إعادة تشغيل إجباري (Hard Reboot) للآلة المستضيفة.
3. بمجرد عمل الخادم، ستعمل الخدمات تلقائياً بفضل سياسة التشغيل المقررة في docker-compose (`restart: always`).
4. تحقق من حالة الحاويات وسجل الأخطاء.

### ب. نسيان أو فقدان كلمة مرور المالك (Owner/Admin Password Reset):
في حال نسيان كلمة مرور الحساب الإداري الرئيسي ولا يمكن تسجيل الدخول للنظام:
1. قم بالدخول إلى حاوية الـ Backend برمجياً:
```bash
docker compose exec backend bash
```
2. قم بتشغيل بيئة بايثون التفاعلية أو تشغيل أمر تحديث المالك:
```bash
python ensure_owner.py --password "NewSecurePassword2026!"
```
3. سيقوم السكربت تلقائياً بتحديث الحساب الإداري وتعيين كلمة المرور الجديدة وتشفيرها بأمان بـ `werkzeug.security`.

### ج. اكتشاف محاولة اختراق أمني أو تسريب بيانات:
1. قم فوراً بإغلاق المنافذ الخارجية للخادم عبر جدار الحماية (Firewall) لمنع أي اتصال خارجي:
```bash
sudo ufw deny proto tcp from any to any port 3000
sudo ufw deny proto tcp from any to any port 443
```
2. قم بمراجعة السجلات المهيكلة JSON في المجلد `/app/logs/app.log` للتعرف على عنوان الـ IP المهاجم ونوع الطلبات المسجلة.
3. قم بسد الثغرة الأمنية، وتغيير كلمات المرور لقاعدة البيانات ومفتاح التشفير `SECRET_KEY` في ملف `.env` وإعادة بناء وتشغيل الحاويات، ثم إعادة فتح جدار الحماية.

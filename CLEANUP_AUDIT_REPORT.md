# تقرير تدقيق وتنظيف النظام من الملفات الزائدة (Cleanup Audit Report)

يوضح هذا المستند خطة العمل المعتمدة لفحص وتطهير مشروع "إدارة معرض السيارات" من أي ملفات مؤقتة، مخلفات بناء، مجلدات اختبار سابقة، أو كود غير مستخدم (Dead Code)، مع الالتزام التام بعدم إتلاف أي ملف حيوي أو كسر النظام.

---

## 1. قائمة المجلدات والملفات المقترح حذفها (Target Candidates)

بعد فحص هيكل المشروع، تم تحديد العناصر التالية كملفات زائدة وغير ضرورية للإنتاج ويمكن حذفها بأمان:

### أ. مجلدات النسخ الاحتياطية القديمة وغير المستخدمة:
- `BACKUP_BEFORE_REVAMP_20260609_2126/`: مجلد نسخة احتياطية قديمة وكبير الحجم.

### ب. مجلدات الاختبارات والتراكمات التجريبية السابقة:
- `user_fix_test_d9v3w76p/`
- `user_fix_test_gqkp0ihz/`
- `user_mgmt_audit2_wuvchtvf/`
- `user_mgmt_audit_20qs8t0j/`

### ج. ملفات ومجلدات الـ Cache والـ Coverage:
- `.pytest_cache/`: مخلفات تشغيل pytest.
- `.next/`: كاش تجميع Next.js القديم (سيتم إعادة بنائه حياً).
- `.coverage`: ملف تغطية الكود القديم للاختبارات.
- مجلدات `__pycache__/` في جميع مسارات المشروع.

### د. ملفات سجلات الأخطاء القديمة (Logs):
- ملفات `.log` القديمة وغير المستخدمة داخل مجلد `logs/`.

---

## 2. قائمة المجلدات والملفات المحظور حذفها نهائياً (Forbidden to Delete)

تخضع الملفات التالية لقواعد أمنية صارمة تمنع إزالتها أو تعديلها حفاظاً على استقرار النظام ومعايير الأمان المالي:

- **مجلدات الأكواد الحية**: `backend/` و `frontend/`.
- **مجلدات الاختبارات**: `tests/`.
- **ملفات إعداد الحاويات**: `docker-compose.yml` و `Dockerfile` لكل من frontend و backend.
- **ملفات أتمتة الـ CI/CD**: المجلد `.github/workflows/`.
- **ملفات التكوين والاعتمادات**: `requirements.txt` و `package.json` و `package-lock.json`.
- **مستندات التوثيق والتقارير الرسمية**: `CLAUDE.md` و `DEPLOYMENT_GUIDE.md` وكافة التقارير المنجزة (مثل `FINAL_PRODUCTION_AUDIT.md`, `GO_LIVE_READINESS_REPORT.md` وغيرها).
- **مجلد التخزين المؤمن المخصص للمؤسسات**: `storage/private/` ومرفقات صور السيارات الحالية.
- **القيود المحاسبية والسجلات المالية**: **حظر قطعي لأي تعديل أو حذف لأي مستند مالي أو محاسبي.**

---

## 3. خطة ومراحل التنفيذ (Execution Steps)

1. **النسخ الاحتياطي والأمان**:
   - تشغيل سكربت بايثون مخصص لإنشاء نسخة احتياطية كاملة للمشروع بصيغة ZIP مضغوطة وحفظها في مجلد `backups/project_backup_before_cleanup.zip` لحماية البيانات قبل أي عملية حذف.
2. **الحذف الفعلي للمجلدات الزائدة**:
   - إزالة المجلدات والملفات المحددة في البند 1 بأمان.
3. **فحص الـ Imports والأكواد غير المستخدمة**:
   - تنظيف الـ imports الزائدة وغير المستعملة في كود الباكيند.
4. **الفحص والتحقق بعد التنظيف**:
   - إعادة تشغيل اختبارات pytest على مجلد `tests/`.
   - فحص سلامة تكوين docker-compose.
   - إعادة بناء الواجهة الأمامية بالكامل للتأكد من خلو البناء من أي مشاكل.
5. **إصدار التقرير النهائي**:
   - تجميع تفاصيل الملفات المحذوفة والمساحات الموفرة وكتابة تقرير التنظيف النهائي `CLEANUP_FINAL_REPORT.md`.

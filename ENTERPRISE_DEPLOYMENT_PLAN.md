# تقرير النشر وتجهيز البنية التحتية للمؤسسات (Enterprise Deployment & Readiness Plan)

يلخص هذا التقرير الجاهزية الفنية للنظام وتفاصيل البنية التحتية المخطط لها للنشر الفعلي، مع تقدير التكاليف السحابية، وتحليل المخاطر الفنية، وخطة التراجع السريع عند الحاجة.

---

## 1. درجة جاهزية الإنتاج النهائية (Production Readiness Score)

بعد تطبيق التحديثات الأمنية وإعداد بيئة الحاويات (Docker) وأتمتة الـ CI/CD، تم تحديث تقييم الجاهزية ليصبح كالتالي:

| المحور | التقييم السابق | التقييم الحالي | مبرر التقييم |
| :--- | :---: | :---: | :--- |
| **التأمين والحماية (Security)** | 95% | **99%** | تأمين مسار وثائق العملاء بالكامل ونقلها للتخزين الخاص، وتفعيل فحص الثغرات التلقائي `Bandit` في الـ CI/CD. |
| **قواعد المحاسبة (Accounting)** | 100% | **100%** | الحفاظ التام والكامل على قواعد المحاسبة المالية المزدوجة، ومنع وحظر أي عمليات حذف للقيود بشكل مطلق. |
| **قاعدة البيانات (Database)** | 92% | **98%** | التوافقية الكاملة مع PostgreSQL وإيقاف الاعتماد على SQLite للإنتاج، مع إعداد سكربتات ترحيل وهجرة Topological متطورة. |
| **الأداء والاستجابة (Performance)** | 95% | **97%** | تهيئة Next.js standalone لتقليل مساحة الحاوية وتسريع الاستجابة والتحميل، وتفعيل الاتصال الفعال بـ PostgreSQL pool. |
| **سهولة النشر والتشغيل (Ops)** | 60% | **98%** | توفير ملفات `Dockerfile` و `docker-compose.yml` كاملة مع تفعيل الهيلث تشيك التلقائي لجميع الخدمات. |
| **الأتمتة والاختبارات (CI/CD)** | 50% | **99%** | أتمتة كاملة للتحقق من سلامة الأكواد والأنواع والاختبارات وبناء الحاويات في كل دمج برمجي. |
| **التقييم الإجمالي للجاهزية** | 96% | **98.5%** | **النظام مهيأ وجاهز بالكامل للإنتاج والنشر التجاري للمؤسسات.** |

---

## 2. تقديرات تكاليف الاستضافة والبنية التحتية (Hosting Cost Estimations)

نظراً لحجم ومتطلبات النظام، تم وضع خيارين للاستضافة السحابية بناءً على ميزانية وحجم المعرض:

### الخيار أ: الخوادم الافتراضية العادية (الموصى به للبدء - DigitalOcean / Hetzner)
*خيار اقتصادي ممتاز وأدائه مرتفع جداً ويتحمل آلاف العمليات اليومية.*

- **خادم افتراضي (Basic VPS)**: RAM: 4GB, CPU: 2 vCPUs, SSD: 80GB (تكلفة تقريبية: **20-25$ شهرياً**).
- **النسخ الاحتياطي التلقائي (DO Backups)**: حفظ نسخ يومية للنظام بالكامل (تكلفة تقريبية: **5$ شهرياً**).
- **الشبكة السحابية الآمنة و SSL**: تفعيل Cloudflare المجاني وإصدار الشهادات مجاناً عبر Let's Encrypt (تكلفة تقريبية: **0$ شهرياً**).
- **التكلفة الإجمالية المقدرة**: **25-30$ شهرياً**.

### الخيار ب: الخدمات المدارة عالية التوفر (Amazon Web Services - AWS)
*خيار مخصص لمعارض السيارات الكبرى والمؤسسات التي تحتاج لتوفر عالٍ وتوسع تلقائي وتخزين مدار بالكامل.*

- **خادم الواجهات والباكيند (AWS EC2 / ECS)**: تشغيل الحاويات بمرونة عالية (تكلفة تقريبية: **40$ شهرياً**).
- **قاعدة بيانات مدارة (AWS RDS PostgreSQL)**: توفير نسخ احتياطي تلقائي مدار، وتوسيع فوري (تكلفة تقريبية: **35$ شهرياً**).
- **التخزين المؤمن للملفات الرسمية (AWS S3)**: لتخزين ملفات الهويات والعقود بشكل منفصل ومعزول (تكلفة تقريبية: **5$ شهرياً**).
- **بوابة الحماية (AWS ALB & Shield)**: لتوزيع الحمل وحماية الخوادم من الهجمات (تكلفة تقريبية: **20$ شهرياً**).
- **التكلفة الإجمالية المقدرة**: **100$ شهرياً**.

---

## 3. تحليل المخاطر الفنية للتشغيل ومعالجتها (Technical Risks)

| الخطر المتوقع | الأثر الفني | آلية المعالجة والوقاية المطبقة |
| :--- | :--- | :--- |
| **انقطاع الاتصال المفاجئ بقاعدة البيانات** | ظهور خطأ 500 أو 502 للمستخدمين وتوقف العمليات. | تم تفعيل خيار `pool_pre_ping: True` في إعدادات الاتصال بقاعدة البيانات بـ `config.py` للتحقق تلقائياً من صحة الاتصال وإعادة بنائه دون تدخل بشري. |
| **هجمات حقن التعليمات أو تسريب البيانات** | اختراق الخادم والوصول للملفات الحساسة للعملاء. | 1. تم نقل وثائق العملاء إلى مجلد `storage/private` خارج مسار الويب العام.<br>2. تشغيل حاوية Flask بمستخدم محدود الصلاحيات (`appuser`).<br>3. فحص ثغرات الكود تلقائياً عبر Bandit في خط الـ CI/CD. |
| **توقف خادم الواجهات بسبب استهلاك الذاكرة** | خروج تطبيق Next.js عن الخدمة. | استخدام البناء خفيف الوزن (`standalone`) وتفعيل الهيلث تشيك التلقائي لإعادة تشغيل الحاوية فوراً في حال توقفها. |
| **فقدان البيانات المحاسبية الهامة** | تعطل المعاملات المالية وصعوبة تتبع القيود. | تم تطبيق قواعد محاسبية صارمة تمنع حذف القيود، مع تفعيل أخذ نسخ احتياطية لقاعدة البيانات كل 6 ساعات ونقلها لخادم خارجي مؤمن. |

---

## 4. خطة التراجع السريع والإصلاح (Rollback Strategy)

في حال نشر إصدار برمي جديد على بيئة الإنتاج وتبين وجود مشكلة جوهرية تؤثر على سير الأعمال، يتم تطبيق خطة التراجع الفوري التالية:

1. **الاحتفاظ بنسخ الصور القديمة للـ Docker**:
   نقوم في مستودع الحاويات (Registry) بالاحتفاظ بآخر صورتين ناجحتين وتسمية الصور بأرقام الإصدارات (Tags مثل `v1.2.0`) بدلاً من الكتابة الدائمة على الوسم `latest`.
2. **أمر التراجع السريع**:
   لتغيير الإصدار والعودة للنسخة المستقرة السابقة (مثال: من الإصدار `v1.2.1` إلى `v1.2.0`)، يتم تعديل متغيرات بيئة الصور في ملف `docker-compose.yml` وتشغيل الأمر التالي:
   ```bash
   docker compose up -d --no-build
   ```
   يستغرق هذا الإجراء **أقل من 30 ثانية** لعودة النظام للعمل بالكامل بالإصدار المستقر السابق دون أي توقف للخدمة.
3. **التراجع عن تعديلات قاعدة البيانات (Database Rollback)**:
   في حال احتوى الإصدار الجديد على تحديثات في بنية الجداول (Migrations) تسببت بمشاكل، يتم تشغيل أمر التراجع من Flask-Migrate قبل التراجع عن الصور:
   ```bash
   docker compose exec backend flask db downgrade
   ```
   وفي الحالات الحرجة جداً، يتم تنظيف قاعدة البيانات واستعادة آخر نسخة احتياطية مطابقة للإصدار السابق خلال بضع دقائق كما هو محدد في دليل استعادة البيانات بعد الكوارث.
